<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- API 호출 (Dio/Firebase). debug/profile에는 이미 선언돼 있지만
         release 빌드는 main만 merge하므로 main에도 명시 필요. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- FCM 푸시 알림 권한 — Android 13(API 33)+ runtime permission.
         미선언 시 OS가 푸시 표시 자체를 차단. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- AdMob Advertising ID. Android 13(API 33)+에서 광고 personalize
         용 ID 접근에 필요. 미선언 시 GMS가 ID를 zeroed 상태로 반환 →
         non-personalized 광고만 노출. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <!-- 퀴즈 음성 입력 (speech_to_text). Android는 텍스트 변환 시
         RecognizerIntent + 마이크 사용. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- speech_to_text는 SpeechRecognizer가 별도 앱(Google 등)으로 분리된
         Android 11+ 패키지 가시성 차단을 우회하기 위해 queries 선언 필요. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:label="LingoLoop"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- AdMob app ID. release/debug 무관 GMS SDK가 manifest에서 직접 읽음. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2707874353926722~1301987304" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- App Links: lingo.jiny.shop의 모든 https URL을 앱이 처리.
                 autoVerify=true 면 OS가 /.well-known/assetlinks.json 을
                 fetch해 SHA256 fingerprint 검증 → 통과한 경우만 disambig
                 화면 없이 곧바로 앱 실행. 통과 못 하면 '브라우저 vs 앱'
                 선택 다이얼로그가 뜸. assetlinks.json의 sha256 list에
                 release 서명 키(upload + Play App Signing 둘 다) 등록 필요. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="lingo.jiny.shop" />
            </intent-filter>
        </activity>
        <!-- Home screen widget: today's sentence -->
        <receiver
            android:name=".SentenceWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sentence_widget_info" />
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Kakao login redirect handler -->
        <activity
            android:name="com.kakao.sdk.flutter.AuthCodeCustomTabsActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="kakao19046586f7f389747bf7093c5f253e9e"
                    android:host="oauth" />
            </intent-filter>
        </activity>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Detect the KakaoTalk app for isKakaoTalkInstalled() -->
        <package android:name="com.kakao.talk" />
    </queries>
</manifest>
