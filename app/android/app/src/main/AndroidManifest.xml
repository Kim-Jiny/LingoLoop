<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- API 호출 (Dio/Firebase). debug/profile에는 이미 선언돼 있지만
         release 빌드는 main만 merge하므로 main에도 명시 필요. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- FCM 푸시 알림 권한 — Android 13(API 33)+ runtime permission.
         미선언 시 OS가 푸시 표시 자체를 차단. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- AdMob Advertising ID. Android 13(API 33)+에서 광고 personalize
         용 ID 접근에 필요. 미선언 시 GMS가 ID를 zeroed 상태로 반환 →
         non-personalized 광고만 노출. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <application
        android:label="LingoLoop"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- AdMob app ID. release/debug 무관 GMS SDK가 manifest에서 직접 읽음. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2707874353926722~1301987304" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Home screen widget: today's sentence -->
        <receiver
            android:name=".SentenceWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sentence_widget_info" />
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Kakao login redirect handler -->
        <activity
            android:name="com.kakao.sdk.flutter.AuthCodeCustomTabsActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="kakao19046586f7f389747bf7093c5f253e9e"
                    android:host="oauth" />
            </intent-filter>
        </activity>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Detect the KakaoTalk app for isKakaoTalkInstalled() -->
        <package android:name="com.kakao.talk" />
    </queries>
</manifest>
