name: lingoloop
description: "Daily sentence-based English learning app."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.2.0+16

environment:
  sdk: ^3.11.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^4.0.2
  go_router: ^17.1.0
  dio: ^5.9.2
  flutter_tts: ^4.2.5
  shared_preferences: ^2.5.4
  flutter_secure_storage: ^10.0.0
  google_fonts: ^8.0.2
  firebase_core: ^4.5.0
  firebase_messaging: ^16.1.2
  firebase_analytics: ^12.0.0
  in_app_purchase: ^3.2.3
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.0
  kakao_flutter_sdk_user: ^1.9.7
  flutter_timezone: ^3.0.1
  share_plus: ^10.1.4
  # 단어장 이미지 export — 위젯 → PNG 캡처 후 OS share sheet로
  # 보내려면 임시 파일 경로가 필요. path_provider는 firebase 등
  # 으로 이미 transitive 의존이지만 직접 import용으로 명시.
  path_provider: ^2.1.5
  # 단어장 이미지 사진첩 저장. iOS는 PhotosUI add-only 권한,
  # Android는 MediaStore (API 30+은 별도 권한 없이 가능). 한 줄
  # API로 양쪽 추상화.
  gal: ^2.3.1
  home_widget: ^0.7.0
  package_info_plus: ^8.0.0
  # 디바이스 모델/OS 버전 수집 — 인증 시점에 서버로 보내 backstage
  # 유저 상세 페이지의 "클라이언트" 행에 노출. iOS는 utsname.machine
  # (e.g. iPhone15,2), Android는 Build.MODEL을 사용.
  device_info_plus: ^11.2.0
  url_launcher: ^6.3.1
  # iOS는 app-settings: scheme, Android는 ACTION_APP_NOTIFICATION_
  # SETTINGS intent를 한 줄 API로 추상화. url_launcher만으론
  # Android app-specific notification settings 못 엶.
  app_settings: ^5.1.1
  # AdMob — 배너 광고 (premium 비활성 사용자 대상). iOS는 별도로
  # GADApplicationIdentifier(Info.plist)와 SKAdNetworkItems 필요.
  google_mobile_ads: ^7.0.0
  # iOS App Tracking Transparency. iOS 14+에서 AdMob personalize
  # 데이터 접근 위해 사용자 권한 다이얼로그 한 번 띄움. Android는
  # no-op 함수만 제공.
  app_tracking_transparency: ^2.0.6
  # 퀴즈 입력 보조: 마이크로 영어 답안 받아쓰기. iOS는 Speech
  # framework + Microphone 권한 두 개, Android는 RECORD_AUDIO.
  # 인식 결과는 텍스트필드에 채워주고 사용자가 보고 제출하는 흐름.
  speech_to_text: ^7.4.0
  # 인앱 리뷰 팝업. iOS는 SKStoreReviewController, Android는 Play
  # In-App Review API를 한 줄 API로 추상화. 둘 다 OS가 연간 호출
  # quota를 강제 throttle. 앱 측에서는 30일 쿨다운 + 설치 2일 이후
  # 만 호출하는 추가 throttle을 ReviewPromptService에서 처리.
  in_app_review: ^2.0.10
  # 네이티브 launch screen에 splash 이미지 박아넣어 cold-start ~ Flutter
  # 첫 프레임 사이 검정 깜빡임을 제거. preserve()/remove() 런타임 API를
  # main()에서 호출하므로 dev_dependencies가 아닌 dependencies.
  flutter_native_splash: ^2.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  image_path: "assets/appIcon.png"
  android: true
  ios: true
  remove_alpha_ios: true
  background_color_ios: "#FBF5EC"

# 네이티브 스플래시. `dart run flutter_native_splash:create` 로 iOS
# LaunchScreen.storyboard + Android launch_background.xml 자동 생성.
#
# 짧은쪽 기준 fill 동작 확보를 위해 `image:`(중앙 작은 로고 패턴)가
# 아니라 `background_image:`(전체 화면 fill 패턴) 사용:
#   · iOS LaunchBackground: scaleToFill — splash.png 짧은쪽 늘어남
#   · Android pre-12: gravity=fill — 동일하게 stretch
# splash.png aspect(864:1821 ≈ 0.475)가 폰 aspect(0.46–0.47)와 거의
# 같아 stretch 왜곡이 시각적으로 무시 가능.
#
# Android 12+ 는 OS가 새 SplashScreen API의 windowSplashScreenAnimatedIcon
# (중앙 아이콘, 최대 ~160dp)만 허용해 background_image 불가 — 어쩔
# 수 없이 image로 작은 중앙 표시.
flutter_native_splash:
  # color와 background_image는 상호배타 (패키지 규칙). background_image
  # 쓸 거라 color는 생략. splash.png가 화면을 stretch fill.
  background_image: assets/splash.png
  fullscreen: true
  android_12:
    # Android 12+ 새 SplashScreen API는 color + icon 구조라 여기는 color
    # 유지. image는 중앙 작은 아이콘으로 노출됨 (OS 제한).
    color: "#000000"
    image: assets/splash.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # splash.png는 flutter_native_splash:create가 네이티브 LaunchScreen/
  # launch_background.xml로 변환해 빌드 시점에만 참조하므로 Flutter
  # assets bundle에 포함 안 함.

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
